- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.config/i3/config: i3
    /etc/i3status.conf: i3status
    ~/.config/i3lock.conf: i3lock.conf

- shell:
    - [git submodule update --init --recursive, Installing submodules]

